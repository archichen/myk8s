apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secretName }}
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  username: {{ .Values.username }}
  password: {{ .Values.password }}
  postgresql-password: {{ .Values.password }}